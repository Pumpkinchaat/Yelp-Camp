<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Farm Details</title>
</head>
<body>
    <h1><%= farm.name %> </h1>
    <h2><%= farm.owner %> </h2>
    <h2><%= farm._id %> </h2>

    <ul>
        <% for (let product of farm.products) {%>
            <li><%= product.name %> - $ <%= product.price %> </li>
        <% } %> 
    </ul>

    <a href="/farms/<%= farm._id %>/new">App a Product</a>
    <a href="/farms">Show all farms</a>
    <form action="/farms/<%= farm._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete This Farm</button>
    </form>
</body>
</html>